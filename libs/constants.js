const LIMIT_NAME_LENGTH = {
  min: 1,
  max: 50,
};

const LIMIT_EMAIL_LENGTH = {
  min: 5,
  max: 50,
};

const LIMIT_PHONE_LENGTH = {
  min: 7,
  max: 30,
};

module.exports = { LIMIT_NAME_LENGTH, LIMIT_EMAIL_LENGTH, LIMIT_PHONE_LENGTH };
